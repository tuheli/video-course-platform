import { Button } from '@mui/material';

export const LoginButton = () => {
  const onClickLogin = () => {};

  return (
    <Button variant="outlined" onClick={onClickLogin}>
      Log in
    </Button>
  );
};
