import { Container, Typography } from '@mui/material';

export const HelpfulResourcesHeader = () => {
  return (
    <Container>
      <Typography
        sx={{
          textAlign: 'center',
        }}
      >
        Based on your experience, we think these resources will be helpful.
      </Typography>
    </Container>
  );
};
